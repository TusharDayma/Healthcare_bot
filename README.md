# 🩺 Healthcare AI Agent (LangChain + Ollama + ChromaDB)

This is a local RAG-based chatbot that can answer medical questions using the Gale Encyclopedia of Medicine.

## 🧠 Tech Stack
- LangChain
- Ollama (`llama3.2`, `nomic-embed-text`)
- ChromaDB
- Python 3.10+

## 🚀 Usage

1. Install dependencies:
```bash
pip install -r requirements.txt
